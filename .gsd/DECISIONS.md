# Decisions Register

<!-- Append-only. Never edit or remove existing rows.
     To reverse a decision, add a new row that supersedes it.
     Read this file at the start of any planning or research phase. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? |
|---|------|-------|----------|--------|-----------|------------|
| D028 | M003 | convention | Number formatting convention for scoring messages | International comma separator (50,000) instead of Indonesian dot (50.000) | User explicitly requested international convention. Consistent with how _fmt_idr already formats currency values with commas. Dot notation was an Indonesian locale artifact that leaked into message templates. | No |
| D029 | M003 | arch | Ads calculator min_cost threshold marketplace scaling | Use IDR_TO_THB_RATE (0.0019) from marketplace.py: IDR 100,000 → THB 190 | Consistent with M001 pattern (migration 026 used same rate for six_month_avg_threshold). Fixed rate appropriate for threshold seeding — admins adjust independently later. | Yes |
| D001 | M001/S01 | data-model | Marketplace column type and constraint pattern | VARCHAR(2) + CHECK constraint (values: 'ID', 'TH') with DEFAULT 'ID' | Matches existing migration 023 pattern. VARCHAR(2) is compact and readable. CHECK constraint enforces validity at DB level. DEFAULT 'ID' ensures all existing rows are backward-compatible. | Yes |
| D002 | M001/S01 | data-model | IDR to THB conversion rate strategy | Fixed rate 0.0019 for initial migration seeding only | Seeding needs a deterministic conversion. Live exchange rates would make the migration non-reproducible. After seeding, admins edit THB thresholds independently — the rate is never used at runtime. | Yes |
| D003 | M001/S01 | data-model | Scoring rules uniqueness constraint with marketplace dimension | UNIQUE(template, marketplace) replacing UNIQUE(template) | Each template must have exactly one rules row per marketplace. The old UNIQUE(template) prevented coexistence of ID and TH rules for the same template. | Yes |
| D004 | M001/S01 | architecture | Backward compatibility strategy for marketplace parameter | All query/service/API functions default marketplace to 'ID' | Every existing caller (frontend, tests, other services) currently operates in the ID marketplace. Defaulting to 'ID' means zero breaking changes — existing code works without modification. | Yes |
| D005 | M001/S02 | architecture | How to handle IDR→{currency} transition in DB-stored message templates | Migration 027 patches ALL scoring_rules rows to use {currency} placeholder in 4 message templates; _format_message_template receives currency=code kwarg at render time | Keeps TestMigrationTemplatesDrift passing (DB templates match DEFAULT_RULES). _SafeDict handles missing {currency} gracefully for any edge case. Both ID and TH rows get the placeholder — the actual currency code is injected at format time via MARKETPLACE_CURRENCY lookup. This is cleaner than having ID rows say IDR and TH rows say {currency}. | Yes |
| D006 | M001/S02 | architecture | Conclusion text (G66) sales range scaling for THB marketplace | THB uses raw comma-formatted numbers (e.g. 150,000 - 250,000); IDR keeps existing juta scaling (e.g. 150 juta - 200 juta) | THB values are ~500x smaller than IDR. Dividing THB values by 1,000,000 yields fractional numbers (0.15 juta) which are meaningless. Raw comma-formatted numbers are clear and appropriate for THB scale. The juta label is IDR-specific Indonesian language. | Yes |
| D007 | M001/S02 | architecture | Currency formatting helper (_fmt_currency) delegates to _fmt_idr — identical formatting for IDR/THB | _fmt_currency(value, marketplace) wraps _fmt_idr; currency CODE comes from template placeholders, not the formatting function | Both IDR and THB use comma-separated thousands formatting. Separating number formatting from currency code injection keeps the formatting function simple and lets the template system handle locale-specific labeling. | Yes |
| D008 | M001/S02 | pattern | Marketplace parameter threading pattern through scoring engine | Thread marketplace as keyword-only param with default "ID" through scoring sub-functions; use MARKETPLACE_CURRENCY.get(marketplace, "IDR") for fallback | Keyword-only params prevent positional arg confusion in functions with many parameters. Default "ID" ensures zero breaking changes for all existing callers. Dict.get() fallback ensures unknown marketplaces degrade gracefully to IDR. | Yes |
| D009 | M001/S02 | architecture | Conclusion text unit scaling varies by marketplace | THB uses raw formatted numbers; IDR uses /1_000_000 with "juta" suffix | Thai Baht values are 3 orders of magnitude smaller than IDR equivalents — the "juta" (million) scaling makes THB values unreadable. Raw numbers are appropriate for THB scale. | Yes |
| D010 | M001/S03 | architecture | Where calculator_service reads marketplace for discount/top_sku calculators | Read from evaluation_inputs (via get_evaluation_inputs) — NOT from brand_vp_data | Migration 026 added marketplace column to evaluation_inputs, evaluations, and scoring_rules — NOT to brand_vp_data. The S03 research doc incorrectly suggested adding v.marketplace to get_brand_by_id (which queries brand_vp_data). The correct source is evaluation_inputs, following the pattern already established by run_ads_keyword_calculator. Fallback to "ID" when eval_inputs is None. | Yes |
| D011 | M001/S03 | architecture | Centralize price parsing in a shared module (price_parser.py) rather than duplicating in each calculator | Single _parse_price(value, marketplace) function in app/calculators/price_parser.py replaces duplicate _clean_price in discount.py and top_sku.py | DRY principle — two identical _clean_price functions existed. Adding marketplace-aware parsing to both independently would compound duplication. Shared module is the extension point for future marketplaces. | Yes |
| D012 | M001/S03 | architecture | Where to source marketplace value in calculator_service.py | Read marketplace from evaluation_inputs (not brand_vp_data) because the marketplace column lives on the evaluation_inputs table | brand_vp_data has no marketplace column. evaluation_inputs stores the per-evaluation marketplace selection (added by S01 migration 026). Same pattern as run_ads_keyword_calculator loading eval_inputs for total_products. | Yes |
| D013 | M001/S03 | library | How to add marketplace parameter to calculator public functions | marketplace is keyword-only (after *) on calculate_discount and calculate_top_sku with default "ID" | Keyword-only avoids positional arg confusion with existing callers. Default "ID" preserves full backward compatibility — no caller needs updating unless they want THB parsing. | Yes |
| D014 | M001/S04 | architecture | How to handle fields.ts unit: 'IDR' on ~16 field definitions | Leave fields.ts unit values as 'IDR' (static config), override at render layer via currency prop on CurrencyField and form components | fields.ts is static configuration used by benchmarkUtils.ts and form rendering. The unit field is display-only, not logic-driving. Changing 16+ field definitions risks benchmark display regressions. The CurrencyField currency prop already overrides the display label. The render layer (BusinessForm, PromoToolsForm, etc.) passes the correct currency based on marketplace context. | Yes |
| D015 | M001/S04 | architecture | How to handle fields.ts unit: 'IDR' on ~16 field definitions | Leave fields.ts unit values as 'IDR' (static config), override at render layer via currency prop on CurrencyField and form components | fields.ts is static configuration used by benchmarkUtils.ts and form rendering. The unit field is display-only, not logic-driving. Changing 16+ field definitions risks benchmark display regressions. The CurrencyField currency prop already overrides the display label. The render layer (BusinessForm, PromoToolsForm, etc.) passes the correct currency based on marketplace context. | Yes |
| D016 | M001/S04 | library | Currency formatting centralized in formUtils.ts with formatCurrency/parseCurrency | Single shared formatCurrency(value, marketplace) function replaces all local formatIDR copies; formatIDR/parseIDR kept as deprecated one-liner re-exports for incremental migration | Eliminates duplication across TopSkuResults, DataIntelligence, EvaluationDetailPage, BusinessForm. Single breakpoint captures all formatting. Deprecated re-exports allow gradual migration without breaking existing callers. | Yes |
| D017 | M001/S04 | architecture | Rules data caching is marketplace-scoped via React Query queryKey | React Query queryKey includes marketplace as second element: ['rules', marketplace ?? 'ID']; onSuccess invalidation uses prefix ['rules'] to refresh all variants | Prevents stale cross-marketplace cache hits. Switching marketplace tabs triggers re-fetch with correct param. Prefix invalidation ensures all marketplace variants refresh after any edit. | Yes |
| D018 | M001/S04 | architecture | Marketplace is explicit user selection, not inferred from brand data | marketplace is user-selectable local state in useEvaluationOrchestrator defaulting to 'ID', not derived from brand data; follows same top-down threading pattern as categoryType | User knows which marketplace a brand belongs to. Explicit selection prevents accidental cross-currency evaluation. Consistent with existing pattern — orchestrator manages state, threads to hooks and UI. | Yes |
| D019 | M002 | architecture | How to render evaluation results in user's language on history detail page | Use renderTranslatable() with stored _i18n keys, falling back to raw Indonesian text when keys are absent | Pattern already proven in dashboard components (CategoryMetricCard, KesimpulanSection). i18n keys already stored in score_breakdown JSONB. Fallback ensures pre-i18n evaluations remain readable. | No |
| D020 | M002 | architecture | Email language is independent of UI language | Email send dialogs have a language selector defaulting to UI language; email body renders via i18n.getFixedT(selectedLang) without changing global UI language | Emails cross language boundaries — Thai staff may send to Indonesian brand owners. Decoupling email language from UI language solves the cross-country scenario. | No |
| D021 | M002 | architecture | Email body rebuilt from i18n keys at render time | Frontend assembles email body from stored i18n structured data at render time in the chosen language; pre-rendered email_output in DB is fallback only | Pre-rendered email_output is always Indonesian. Dynamic assembly from i18n keys is the only way to support language switching without storing N copies per language. | No |
| D022 | M002 | convention | Locale files use {{currency}} variable for all currency references | Replace hardcoded "IDR" in 6 locale keys across all 3 locale files with {{currency}} interpolation variable | Backend already provides currency in i18n vars. Hardcoded IDR produces incorrect output for THB marketplace evaluations. | No |
| D023 | M002/S02 | architecture | Evaluation detail API marketplace field fallback strategy | Triple-layer fallback: SQL COALESCE(e.marketplace, 'ID') → service row.get("marketplace", "ID") → Pydantic default "ID" | Three independent layers ensure marketplace is never absent from the API response. SQL COALESCE handles NULL in DB, service-level get() handles missing dict key, Pydantic default handles missing constructor arg. Defense in depth for a field that downstream i18n rendering depends on. | Yes |
| D024 | M002/S02 | pattern | Where to define ScoringConclusionSection component for evaluation detail scoring summary | ScoringConclusionSection defined inline in EvaluationDetailPage.tsx alongside other local section components | Matches the established local component pattern in EvaluationDetailPage.tsx where ScoreBreakdownTable, AdsKeywordSection, TopSkuSection, and DiscountSection are all inline. Keeps scoring_summary consumption co-located with the page that renders it. Extraction to a separate file can happen if the component grows or gets reused. | Yes |
| D025 | M002/S02 | pattern | How to translate Indonesian backend category names in frontend table components | CATEGORY_MAP.find(m => m.backend === categoryString) + t(mapped.label) with raw-string fallback for unmatched categories | Backend stores category names in Indonesian (e.g. "Kesehatan Operasional Toko"). CATEGORY_MAP provides a stable mapping from backend strings to i18n label keys. Fallback to raw string ensures unknown or future categories render visibly rather than erroring. Pattern is reusable by any component rendering backend category names. | Yes |
| D026 | M002/S03 | pattern | How to render email section headers in translated email body | Dedicated emailBody.section.* locale keys instead of reusing rules.category.* keys | Backend email section headers differ from category names (e.g. "Store Operational Performance" vs "Operations"). Reusing rules.category.* would produce incorrect email formatting. Separate keys allow email headers to match the backend _assemble_email_body output exactly. | Yes |
| D027 | M002/S03 | pattern | How email language selector handles pre-i18n evaluations | EmailLanguageSelector renders conditionally — only when scoringResult or i18n data is present; hasI18nData guard checks for message_i18n on score_breakdown rows before enabling language-aware rendering | Pre-i18n evaluations lack _i18n fields, so language switching has no effect on their email body. Hiding or disabling the selector avoids confusing users. In EmailOutput, the selector is hidden when scoringResult is absent. In SendMailDialog, the selector always renders but the hasI18nData guard causes it to fall back to the original Indonesian body when no i18n data exists. | Yes |
| D030 | M004 | convention | Month abbreviations language | English month abbreviations (Jan, Feb, Mar, ..., Dec) for all languages | User explicitly requested English months across all languages. Simplifies i18n — no per-locale month name arrays needed. Only 4 values change from Indonesian: Mei→May, Agu→Aug, Okt→Oct, Des→Dec. | Yes — if a future locale requires native month names |
| D031 | M004 | pattern | Field label i18n strategy for fields.ts | Change field.label from hardcoded Indonesian to i18n key (field.labelKey), resolve with t() at render time in form components | fields.ts labels are display-only — form components render them directly. Adding a labelKey property preserves backward compatibility during migration. 8 form components updated to resolve t(field.labelKey) instead of using field.label. | Yes |
| D032 | M004/S02 | pattern | How to handle module-level Intl.DateTimeFormat when locale reactivity is needed | Convert module-level const to useMemo inside component with i18n.language dependency | Module-level Intl.DateTimeFormat('id-ID') is initialized once at import time and never reacts to language changes. Wrapping in useMemo with [i18n.language] dependency re-creates the formatter when the user switches locale. Applied to AccountsPage dateFormatter as the reference pattern. | Yes |
| D033 | M004/S03 | pattern | How to i18n field-level benchmark descriptions and unit labels in fields.ts | Add optional benchmarkKey and unitKey to FieldDefinition; form components resolve via t(field.benchmarkKey) with fallback to field.benchmark/field.unit | Same pattern as labelKey (D031). benchmarkKey covers 10 PROMO_TOOLS_FIELDS benchmark descriptions like ">8% dari penjualan". unitKey covers operational field units like "hari". Optional keys preserve backward compatibility — fields without keys continue using hardcoded values. | Yes |
| D034 | M004/S03 | architecture | How DetailedEvaluation metric comparisons work across old and new evaluations | Check row.metric_i18n?.key first for i18n key match, then fall back to row.metric string comparison for pre-M002 evaluations | Post-M002 evaluations include metric_i18n with i18n keys (e.g. scoring.adCost). Pre-M002 evaluations have only Indonesian metric strings. The \|\| fallback ensures both old and new evaluations render correctly in the dashboard without data migration. | Yes — can remove row.metric fallback once all pre-M002 evaluations age out |
| D035 | M005/S01 | pattern | How to render ads keyword content on evaluation detail page | Reuse DataIntelligence.tsx AdsContent pattern — render from structured i18n details (ak2_i18n, al2_i18n, etc.) with output_text fallback | DataIntelligence.tsx already solves this for the dashboard. Same data, same utilities. No new infrastructure. | No |
| D036 | M005/S01 | pattern | How to render discount content on evaluation detail page | Render from score_breakdown row 73's value_i18n (scoring.discountCheckup.pass/fail keys) with output_text fallback | Backend already generates value_i18n on discount row 73 with structured interpolation vars. Locale keys exist in all 3 languages. Different path than ads keyword (score_breakdown vs calculator_results.details). | No |
| D037 | M005/S02 | pattern | How to render email output section on evaluation detail page | Use buildI18nEmailBody with score_breakdown + scoring_summary, same as SendMailDialog; fall back to raw email_output for pre-i18n evaluations | buildI18nEmailBody.ts already exists and is proven in SendMailDialog. hasI18nData guard pattern established there. Reuse prevents duplication. | No |
