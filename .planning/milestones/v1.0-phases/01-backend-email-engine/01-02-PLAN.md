---
phase: 01-backend-email-engine
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/modules/email/template.py
  - backend/tests/unit/email/test_template.py
autonomous: true
requirements:
  - INFRA-04
  - CONT-01
  - CONT-02
  - CONT-04
  - CONT-05

must_haves:
  truths:
    - "Email HTML uses table-based layout with inline CSS on every element (no style blocks)"
    - "Score overview shows large score number, colored progress bar, check/cross counts, and template type"
    - "Detailed evaluation shows all categories stacked vertically with all metric cards (name, value, benchmark, verdict, score, message)"
    - "Data intelligence section shows Ads Analysis as preformatted text and Top SKU as revenue + stock ranking tables (top 3)"
    - "Email renders with responsive structure (2-col desktop, 1-col mobile via media queries where supported)"
  artifacts:
    - path: "backend/app/modules/email/template.py"
      provides: "render_email_html() and all section renderers, STRINGS dict, CATEGORY_MAP"
      exports: ["render_email_html"]
      min_lines: 200
    - path: "backend/tests/unit/email/test_template.py"
      provides: "Tests for HTML output: score overview, detailed evaluation, data intelligence, responsive structure"
      min_lines: 100
  key_links:
    - from: "backend/app/modules/email/template.py"
      to: "EvaluationDetailResponse shape"
      via: "evaluation_data dict parameter"
      pattern: "evaluation_data"
    - from: "backend/app/modules/email/template.py"
      to: "CID references"
      via: "header_cid, footer_cid, chart_cid parameters in render_email_html"
      pattern: "cid:"
---

<objective>
Build the complete HTML email template with all content sections: score overview, detailed evaluation, score breakdown chart area, data intelligence, header/footer — all using table-based layout with inline CSS for cross-client email compatibility.

Purpose: This is the content engine. It takes evaluation data + CID references and produces the full HTML email body. Pure functions, no I/O, fully testable.
Output: template.py with render_email_html() that produces cross-client-compatible HTML, plus comprehensive tests.
</objective>

<execution_context>
@/Users/mac/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mac/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-backend-email-engine/01-CONTEXT.md
@.planning/phases/01-backend-email-engine/01-RESEARCH.md
@.planning/phases/01-backend-email-engine/01-VALIDATION.md

<interfaces>
<!-- The template receives evaluation data matching EvaluationDetailResponse shape -->

From backend/app/modules/evaluations/schemas.py:
```python
class EvaluationDetailResponse(BaseModel):
    id: int
    brand_id: int
    brand_name: str
    final_score: float          # 0-100
    verdict: str                # "✔️" or "❌" variants
    template: str               # "fashion" or "non_fashion"
    score_breakdown: list[dict[str, Any]]  # list of CategoryScoreItem-like dicts
    calculator_results: dict[str, Any]     # {"ads_keyword": {...}, "top_sku": {...}, "discount": {...}}
    period: str                            # e.g. "Januari 2026"

# CategoryScoreItem shape (inside score_breakdown):
# {
#   "category": str,           # Full Indonesian category name
#   "score": float,
#   "max_score": float,
#   "available": bool,
#   "rows": [
#     {
#       "row": int,
#       "metric": str,         # Indonesian metric name
#       "value": Any,
#       "benchmark": str,
#       "verdict": str,        # "✔️" or "❌"
#       "message": str,
#       "score": float
#     }
#   ]
# }

# calculator_results shape:
# {
#   "ads_keyword": {"output_text": str, "details": dict},
#   "top_sku": {"output_text": str, "details": {"revenue_ranking": [...], "stock_ranking": [...]}},
#   "discount": {"output_text": str, "details": dict}
# }
```

From frontend categoryMap.ts (mirror in Python):
```python
CATEGORY_MAP = {
    "Kesehatan Operasional Toko": "Operasional",
    "Bisnis Analisis": "Bisnis",
    "Tinjauan Pengunjung": "Pengunjung",
    "Promo Toko": "Alat Promo",
    "Jumlah Produk & Status Toko": "Produk & Status",
    "Data Iklan": "Iklan",
    "Partisipasi Campaign": "Campaign",
    "Kompetisi TOP Produk": "Kompetisi",
    "Stok": "Stok",
    "Discount": "Diskon",
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Score overview and header/footer template sections</name>
  <files>
    backend/app/modules/email/template.py,
    backend/tests/unit/email/test_template.py
  </files>
  <behavior>
    - Test: render_email_html returns string containing DOCTYPE html
    - Test: HTML contains no style blocks (no &lt;style&gt; tags) -- all inline
    - Test: HTML uses table elements for layout (role="presentation")
    - Test: Header section contains img tag with cid:{header_cid}
    - Test: Footer section contains img tag with cid:{footer_cid}
    - Test: Brand name and period appear below header image
    - Test: Score overview shows final_score as large number
    - Test: Score overview has progress bar with correct color (green >= 80, blue >= 50, orange < 50)
    - Test: Score overview shows check count and cross count from verdict counting
    - Test: Score overview shows template type
    - Test: Score overview shows verdict text
    - Test: STRINGS dict contains Indonesian translations for all section headers
    - Test: CATEGORY_MAP maps full category names to short display names
  </behavior>
  <action>
    Create backend/app/modules/email/template.py with:

    1. STRINGS dict with "id" key containing all Indonesian UI strings (section headers, column labels, verdict labels). Mirror frontend id.json where applicable.

    2. CATEGORY_MAP dict mapping full category names to short display names (from frontend categoryMap.ts).

    3. COLOR constants: PRIMARY_BLUE = "#1976D2", GREEN = "#4CAF50", ORANGE = "#FF9800", BG_GRAY = "#f5f5f5", WHITE = "#ffffff", TEXT_DARK = "#212121", TEXT_SECONDARY = "#757575", CARD_BG = "#FAFAFA". Match dashboard theme per user decision.

    4. Helper function `_score_color(score: float) -> str` — returns GREEN if >= 80, PRIMARY_BLUE if >= 50, ORANGE otherwise.

    5. Helper function `_compute_verdict_counts(categories: list[dict]) -> dict` — counts check marks and cross marks across all rows in all categories. Returns {"checks": int, "xs": int, "total": int}.

    6. Function `_render_header(header_cid: str, brand_name: str, period: str, verdict: str, template: str) -> str` — table-based header with:
       - Full-width header image via CID
       - Below image: brand name (large, bold), period, verdict badge, template type
       - All inline CSS, table layout

    7. Function `_render_footer(footer_cid: str) -> str` — full-width footer image via CID.

    8. Function `_render_score_overview(final_score: float, verdict: str, template: str, categories: list[dict]) -> str` — table-based section with:
       - Section header "Ringkasan Skor"
       - Large score number (styled with _score_color)
       - Horizontal progress bar using nested tables (per research HTML pattern)
       - Check/cross counts from _compute_verdict_counts
       - Template type label
       - All inline CSS

    9. Main function `render_email_html(*, evaluation_data: dict, chart_cid: str, header_cid: str, footer_cid: str) -> str`:
       - Outer wrapper table (100% width, gray background)
       - Content table (600px max, white background)
       - Calls: _render_header, _render_score_overview (implemented in this task)
       - Placeholder calls for: _render_detailed_evaluation, _render_score_breakdown, _render_data_intelligence (implement as stub returning empty string -- Task 2 fills these in)
       - Calls: _render_footer
       - NO style blocks -- all inline CSS
       - Responsive: uses `style="width:100%; max-width:600px;"` pattern

    Create backend/tests/unit/email/test_template.py. NOTE: This test file needs its own fixture for evaluation data since Plan 01's conftest.py may not exist yet (parallel wave). Create a local fixture in the test file OR create a minimal conftest.py in tests/unit/email/ if it doesn't exist yet (check first). Tests for:
       - HTML structure (DOCTYPE, no style blocks, table layout)
       - Header/footer CID references
       - Score overview content and color thresholds
       - STRINGS dict completeness
       - CATEGORY_MAP entries
       - Verdict counting logic
  </action>
  <verify>
    <automated>cd /Users/mac/HT/Project/aha_sicu/backend && uv run pytest tests/unit/email/test_template.py -x -v -k "not detailed and not intelligence and not responsive and not breakdown"</automated>
  </verify>
  <done>
    - render_email_html produces valid HTML with table layout and inline CSS only
    - Header image embedded via CID reference
    - Footer image embedded via CID reference
    - Score overview shows score, progress bar with correct color, verdict counts
    - STRINGS dict and CATEGORY_MAP defined
    - All template structure tests pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Detailed evaluation, score breakdown, and data intelligence sections</name>
  <files>
    backend/app/modules/email/template.py,
    backend/tests/unit/email/test_template.py
  </files>
  <behavior>
    - Test: Detailed evaluation section renders all categories from score_breakdown
    - Test: Each category shows category name (short display name from CATEGORY_MAP)
    - Test: Each metric card shows: metric name, value, benchmark, verdict, score, message
    - Test: 2-column grid structure in HTML for metric cards (using table with 2 td per tr)
    - Test: Score breakdown section shows chart image via cid:{chart_cid}
    - Test: Score breakdown shows category bars with progress bars and check/cross counts
    - Test: Data intelligence section renders ads_keyword output_text as preformatted text
    - Test: Data intelligence section renders top_sku revenue ranking table (top 3 rows)
    - Test: Data intelligence section renders top_sku stock ranking table (top 3 rows)
    - Test: Data intelligence handles missing calculator_results gracefully (no crash)
    - Test: Responsive media query present for mobile single-column layout
    - Test: Full render_email_html produces complete HTML with all sections populated
  </behavior>
  <action>
    Update backend/app/modules/email/template.py, replacing stub functions with full implementations:

    1. Function `_render_detailed_evaluation(categories: list[dict]) -> str`:
       - Section header "Evaluasi Detail"
       - For each category in score_breakdown:
         - Category header with short name from CATEGORY_MAP (fallback to full name)
         - Category score as "X / Y" with progress bar
         - Metric cards in 2-column table grid (2 td elements per tr, each td is a metric card)
         - Each metric card: metric name (bold), value, benchmark, verdict (colored), score, message
         - All inline CSS, table-based layout
         - On last row if odd number of metrics, single td with colspan=2 or empty td

    2. Function `_render_score_breakdown(chart_cid: str, categories: list[dict]) -> str`:
       - Section header "Rincian Skor"
       - Chart image via `<img src="cid:{chart_cid}" width="600" style="display:block; width:100%; height:auto;">`
       - Below chart: category summary bars for each category
         - Short name, score/max_score, progress bar (table-based), check/cross count per category

    3. Function `_render_data_intelligence(calculator_results: dict) -> str`:
       - Section header "Data Inteligensi"
       - If "ads_keyword" in calculator_results:
         - Sub-header "Analisis Iklan"
         - output_text rendered in preformatted style (monospace font, white-space: pre-wrap, background card)
       - If "top_sku" in calculator_results:
         - Sub-header "Top SKU"
         - Revenue ranking table: header row + top 3 data rows from details.revenue_ranking (or details["revenue_ranking"])
         - Stock ranking table: header row + top 3 data rows from details.stock_ranking
         - Tables with inline CSS borders, padding, alternating row colors
       - Gracefully handle missing keys (return empty section, no crash)

    4. Update render_email_html to call the real functions instead of stubs.

    5. Add a minimal responsive `<style>` block ONLY inside `<head>` for media queries (Gmail strips these but mobile Gmail app may support some). Critical layout must NOT depend on these -- they are progressive enhancement only. The inline CSS on tables must produce acceptable layout without media queries.

    Update backend/tests/unit/email/test_template.py adding tests for:
       - Detailed evaluation section with multiple categories
       - Metric card content verification
       - Score breakdown with chart CID
       - Data intelligence with ads + top_sku
       - Data intelligence with missing calculator results
       - Full HTML output completeness check
       - Responsive structure (media query presence as progressive enhancement)
  </action>
  <verify>
    <automated>cd /Users/mac/HT/Project/aha_sicu/backend && uv run pytest tests/unit/email/test_template.py -x -v</automated>
  </verify>
  <done>
    - Detailed evaluation renders all categories with all metric fields
    - 2-column grid layout for metric cards using tables
    - Score breakdown shows chart image via CID and category summary bars
    - Data intelligence shows ads analysis (preformatted) and top SKU tables (top 3)
    - Missing calculator_results handled gracefully
    - Full render_email_html produces complete email HTML
    - All template tests pass
  </done>
</task>

</tasks>

<verification>
cd /Users/mac/HT/Project/aha_sicu/backend && uv run pytest tests/unit/email/test_template.py -x -v && uv run ruff check app/modules/email/template.py
</verification>

<success_criteria>
- render_email_html produces complete HTML email with all 5 sections (header, score overview, detailed evaluation, score breakdown, data intelligence, footer)
- All layout uses tables with inline CSS (no style block dependencies)
- Score colors match thresholds: green >= 80%, blue >= 50%, orange < 50%
- Category names use CATEGORY_MAP short names
- Data intelligence handles missing data gracefully
- All metric fields rendered (name, value, benchmark, verdict, score, message)
- Top SKU tables limited to 3 rows each
- All unit tests pass, ruff clean
</success_criteria>

<output>
After completion, create `.planning/phases/01-backend-email-engine/01-02-SUMMARY.md`
</output>
