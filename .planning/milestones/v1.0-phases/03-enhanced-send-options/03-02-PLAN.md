---
phase: 03-enhanced-send-options
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - frontend/src/components/dashboard/EmailChipInput.tsx
  - frontend/src/components/dashboard/EmailChipInput.test.tsx
  - frontend/src/components/dashboard/SendEmailDialog.tsx
  - frontend/src/components/dashboard/SendEmailDialog.test.tsx
  - frontend/src/hooks/useSendEmail.ts
  - frontend/src/services/apiClient.ts
  - frontend/src/locales/id.json
requirements: [SEND-03, SEND-04, SEND-05, CONT-07]
autonomous: false

must_haves:
  truths:
    - "User can type email and press Enter or comma to create a chip"
    - "User can remove a chip via X button or Backspace on empty input"
    - "PIC email is pre-filled as first chip in To field"
    - "Invalid email format rejected immediately with red border and brief message"
    - "User can expand CC and BCC fields via text links next to To label"
    - "CC/BCC fields use same chip input component as To"
    - "Total recipients across To+CC+BCC capped at 10"
    - "Send button disabled until at least one valid To recipient"
    - "User can type a custom note with 500-char limit and live character count"
    - "User can expand preview section to see backend-rendered email HTML in iframe"
    - "Preview reflects current custom note via query parameter"
    - "Header/footer images visible in preview (data URIs from backend)"
    - "Chart shows placeholder in preview"
  artifacts:
    - path: "frontend/src/components/dashboard/EmailChipInput.tsx"
      provides: "Reusable tag/chip input component for email addresses"
      exports: ["EmailChipInput", "EmailChipInputProps"]
    - path: "frontend/src/components/dashboard/EmailChipInput.test.tsx"
      provides: "Unit tests for EmailChipInput component"
      min_lines: 40
    - path: "frontend/src/components/dashboard/SendEmailDialog.tsx"
      provides: "Refactored dialog with chip inputs, CC/BCC, note textarea, preview iframe"
      contains: "EmailChipInput"
    - path: "frontend/src/hooks/useSendEmail.ts"
      provides: "Updated mutation hook with recipients/cc/bcc/note params"
      contains: "recipients"
    - path: "frontend/src/services/apiClient.ts"
      provides: "Updated path types for /api/v1/email/send with new schema"
      contains: "recipients"
  key_links:
    - from: "frontend/src/components/dashboard/SendEmailDialog.tsx"
      to: "frontend/src/components/dashboard/EmailChipInput.tsx"
      via: "import and render for To/CC/BCC fields"
      pattern: "EmailChipInput"
    - from: "frontend/src/components/dashboard/SendEmailDialog.tsx"
      to: "frontend/src/hooks/useSendEmail.ts"
      via: "mutate call with recipients/cc/bcc/note"
      pattern: "mutate.*recipients"
    - from: "frontend/src/hooks/useSendEmail.ts"
      to: "frontend/src/services/apiClient.ts"
      via: "client.POST with updated body schema"
      pattern: "client.POST.*email/send"
    - from: "frontend/src/components/dashboard/SendEmailDialog.tsx"
      to: "/api/v1/email/preview/{evaluation_id}"
      via: "plain fetch for preview HTML with note query param"
      pattern: "fetch.*preview"
---

<objective>
Build EmailChipInput component and refactor SendEmailDialog to support multiple recipients with CC/BCC, custom note textarea, and in-dialog email preview.

Purpose: Users get full control over email recipients and content before sending -- the final piece of the email feature.
Output: Complete enhanced send dialog with all Phase 3 UI features.
</objective>

<execution_context>
@/Users/mac/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mac/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-enhanced-send-options/03-CONTEXT.md
@.planning/phases/03-enhanced-send-options/03-RESEARCH.md
@.planning/phases/03-enhanced-send-options/03-01-SUMMARY.md

<interfaces>
<!-- Backend schema after Plan 01 (expected shape) -->

POST /api/v1/email/send request body:
```json
{
  "evaluation_id": 1,
  "recipients": ["a@example.com", "b@example.com"],
  "cc": ["cc@example.com"],
  "bcc": ["bcc@example.com"],
  "chart_image": "base64...",
  "subject": null,
  "note": "Custom note text"
}
```

POST /api/v1/email/send response:
```json
{
  "success": true,
  "message_id": "...",
  "recipients": ["a@example.com", "b@example.com"]
}
```

GET /api/v1/email/preview/{evaluation_id}?note=Custom+note
Returns: raw HTML string (not JSON)

From frontend/src/components/dashboard/SendEmailDialog.tsx (CURRENT -- will be refactored):
```typescript
interface SendEmailDialogProps {
  open: boolean;
  onOpenChange: (open: boolean) => void;
  evaluationId: number;
  brandName: string;
  period: string;
  score: number;
  brandRawData: BrandRawData;
  chartRef: React.RefObject<HTMLDivElement | null>;
  onSuccess?: () => void;
}
const isValidEmail = (email: string) => /^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email);
```

From frontend/src/hooks/useSendEmail.ts (CURRENT -- will be updated):
```typescript
export interface SendEmailParams {
  evaluationId: number;
  recipient: string;
  chartImage: string;
}
```

From frontend/src/services/apiClient.ts (CURRENT email path -- will be updated):
```typescript
'/api/v1/email/send': {
  post: {
    requestBody: { content: { 'application/json': {
      evaluation_id: number;
      recipient: string;
      chart_image: string;
      subject?: string | null;
    }}};
    responses: { 200: { content: { 'application/json': {
      success: boolean;
      message_id: string;
      recipient: string;
    }}}};
  };
};
```

From frontend/src/locales/id.json (existing sendEmail keys):
```
"sendEmail.title", "sendEmail.recipient", "sendEmail.recipientPlaceholder",
"sendEmail.send", "sendEmail.sending", "sendEmail.retry", "sendEmail.cancel",
"sendEmail.sendButton", "sendEmail.success", "sendEmail.error", "sendEmail.captureError"
```

Existing UI components available:
- Dialog, DialogContent, DialogHeader, DialogFooter, DialogTitle from '../ui/dialog'
- Button from '../ui/button'
- Input from '../ui/input'
- Label from '../ui/label'
- Card, CardContent from '../ui/card'
- lucide-react icons: X, ChevronDown, Eye, Mail, Loader2
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create EmailChipInput component and update API client + hook</name>
  <files>
    frontend/src/components/dashboard/EmailChipInput.tsx,
    frontend/src/components/dashboard/EmailChipInput.test.tsx,
    frontend/src/hooks/useSendEmail.ts,
    frontend/src/services/apiClient.ts
  </files>
  <behavior>
    EmailChipInput:
    - Renders chips for each email in emails array prop
    - Each chip shows email text and X button to remove
    - Input field sits inline after chips (flexbox wrap)
    - Pressing Enter adds current input as chip (if valid email)
    - Pressing comma adds current input as chip (prevent default)
    - Pressing Backspace on empty input removes last chip
    - Invalid email shows red border + brief error message below
    - Duplicate email shows error message
    - Respects maxTotal/currentTotal -- cannot add when at capacity
    - onBlur adds current input as chip (if non-empty and valid)
    - Placeholder shown only when no chips exist

    useSendEmail:
    - SendEmailParams has recipients: string[], cc?: string[], bcc?: string[], note?: string
    - Sends to /api/v1/email/send with updated body shape

    apiClient:
    - Path type updated: recipient -> recipients (list), add cc, bcc, note fields
    - Response type updated: recipient -> recipients (list)
  </behavior>
  <action>
    **EmailChipInput.tsx:** Create a controlled component with props: `{ id, emails, onChange, disabled?, placeholder?, maxTotal?, currentTotal? }`. Use `useState` for `inputValue` and `error`. Implement:
    - `addEmail(raw: string)`: trim, strip trailing comma, validate with `isValidEmail` regex (`/^[^\s@]+@[^\s@]+\.[^\s@]+$/`), check duplicate, check capacity, call `onChange([...emails, email])`, clear input and error.
    - `handleKeyDown`: Enter or comma -> `e.preventDefault()` + `addEmail(inputValue)`. Backspace on empty input -> `onChange(emails.slice(0, -1))`.
    - `onBlur`: if inputValue non-empty, call `addEmail(inputValue)`.
    - Render: outer div with `flex flex-wrap items-center gap-1 rounded-md border p-1.5 min-h-[38px]` (add `border-destructive` when error). Map chips as `<span>` with `inline-flex items-center gap-1 rounded bg-muted px-2 py-0.5 text-sm`. X button with `<X className="size-3" />` from lucide-react. Input with `flex-1 min-w-[120px] outline-none bg-transparent text-sm`. Error `<p>` below with `w-full text-xs text-destructive mt-1`.
    - Use `useTranslation` for error messages. Add i18n keys in Task 2.
    - Export both `EmailChipInput` and `EmailChipInputProps` interface.

    **EmailChipInput.test.tsx:** Test all behaviors listed above using React Testing Library. Mock `useTranslation` same pattern as SendEmailDialog.test.tsx. Test: add via Enter, add via comma, remove via X click, remove via Backspace, reject invalid email, reject duplicate, respect max capacity, onBlur adds email, placeholder only when empty.

    **useSendEmail.ts:** Update `SendEmailParams` interface: replace `recipient: string` with `recipients: string[]`, add `cc?: string[]`, `bcc?: string[]`, `note?: string`. Update `mutationFn` body: `recipients` instead of `recipient`, conditionally include `cc`, `bcc`, `note`.

    **apiClient.ts:** Update `/api/v1/email/send` path type:
    - Request body: replace `recipient: string` with `recipients: string[]`, add `cc?: string[]`, `bcc?: string[]`, `note?: string | null`.
    - Response: replace `recipient: string` with `recipients: string[]`.
  </action>
  <verify>
    <automated>cd /Users/mac/HT/Project/aha_sicu/frontend && npx vitest run src/components/dashboard/EmailChipInput.test.tsx --reporter=verbose</automated>
  </verify>
  <done>
    - EmailChipInput component handles Enter/comma to add, Backspace/X to remove, validates format, rejects duplicates, respects max capacity
    - useSendEmail sends recipients/cc/bcc/note to backend
    - apiClient path types match new backend schema
    - All EmailChipInput tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Refactor SendEmailDialog with CC/BCC, note textarea, preview iframe, and i18n</name>
  <files>
    frontend/src/components/dashboard/SendEmailDialog.tsx,
    frontend/src/components/dashboard/SendEmailDialog.test.tsx,
    frontend/src/locales/id.json
  </files>
  <action>
    **SendEmailDialog.tsx -- full refactor:**

    State additions:
    - `recipients: string[]` initialized with `[brandRawData.email].filter(Boolean)` (replaces single `recipient` string)
    - `cc: string[]` initialized empty
    - `bcc: string[]` initialized empty
    - `showCc: boolean` = false
    - `showBcc: boolean` = false
    - `note: string` = ""
    - `showPreview: boolean` = false
    - `previewHtml: string | null` = null
    - `previewLoading: boolean` = false

    Computed values:
    - `totalRecipients = recipients.length + cc.length + bcc.length`
    - `sendDisabled = recipients.length === 0 || isPending`

    Recipient section:
    - To label row: `<div className="flex items-center gap-2"><Label>To</Label>` + conditionally render CC/BCC text links: `{!showCc && <button className="text-xs text-muted-foreground hover:text-foreground" onClick={() => setShowCc(true)}>CC</button>}` same for BCC.
    - `<EmailChipInput id="send-email-to" emails={recipients} onChange={setRecipients} disabled={isPending} placeholder={t('sendEmail.recipientPlaceholder')} maxTotal={10} currentTotal={totalRecipients} />`
    - If showCc: Label "CC" + EmailChipInput for cc
    - If showBcc: Label "BCC" + EmailChipInput for bcc

    Note section (below recipient fields, above brand summary card):
    - Label "Catatan (opsional)" using `t('sendEmail.noteLabel')`
    - `<textarea>` with value={note}, onChange, maxLength=500, disabled={isPending}, className with resize-none, rows=3
    - Character count: `<p className="text-xs text-muted-foreground text-right">{note.length}/500</p>`

    Brand summary card: Keep existing Card with brandName, period, score.

    Preview section (below brand summary card):
    - Toggle button: `<button onClick={togglePreview} className="flex items-center gap-1 text-sm text-muted-foreground hover:text-foreground">` with ChevronDown icon (rotated when open) and text `t('sendEmail.previewToggle')` ("Pratinjau Email").
    - When showPreview is true and previewHtml is null, fetch preview. Use plain `fetch` (not openapi-fetch client) since preview returns raw HTML:
      ```
      const baseUrl = import.meta.env.VITE_API_BASE_URL || '';
      const noteParam = note ? `?note=${encodeURIComponent(note)}` : '';
      const res = await fetch(`${baseUrl}/api/v1/email/preview/${evaluationId}${noteParam}`, {
        headers: { Authorization: `Bearer ${await getCurrentUserToken()}` }
      });
      const html = await res.text();
      setPreviewHtml(html);
      ```
      Import `getCurrentUserToken` from `../../firebase/auth`. Show loading spinner while fetching.
    - Render: `<iframe srcdoc={previewHtml} className="w-full h-[300px] border rounded" sandbox="allow-same-origin" />` when previewHtml is loaded.
    - When note changes and preview is visible, debounce refetch (simple approach: clear previewHtml on note change so it refetches on next render cycle, or use a refresh button). Simplest: add a "Muat Ulang" (Refresh) button next to preview toggle that clears previewHtml to trigger refetch.

    Dialog scrollability: Add `max-h-[85vh] overflow-y-auto` to the content wrapper div inside DialogContent to handle overflow when all fields are expanded.

    handleClose reset: Reset all new state (recipients back to initial, cc/bcc empty, showCc/showBcc false, note empty, showPreview false, previewHtml null).

    handleSend: Update mutate call to pass `{ evaluationId, recipients, cc: cc.length ? cc : undefined, bcc: bcc.length ? bcc : undefined, chartImage, note: note || undefined }`. Update onSuccess toast to show recipient count: `t('sendEmail.successMultiple', { count: recipients.length })`.

    Dialog width: Change `sm:max-w-lg` to `sm:max-w-xl` to accommodate wider content.

    **id.json -- add new translation keys:**
    ```
    "sendEmail.noteLabel": "Catatan (opsional)",
    "sendEmail.notePlaceholder": "Tambahkan catatan untuk penerima...",
    "sendEmail.previewToggle": "Pratinjau Email",
    "sendEmail.previewRefresh": "Muat Ulang",
    "sendEmail.previewLoading": "Memuat pratinjau...",
    "sendEmail.invalidEmail": "Format email tidak valid",
    "sendEmail.duplicateEmail": "Email sudah ditambahkan",
    "sendEmail.maxRecipients": "Maksimal 10 penerima",
    "sendEmail.successMultiple": "Email terkirim ke {{count}} penerima",
    "sendEmail.cc": "CC",
    "sendEmail.bcc": "BCC"
    ```
    Keep existing keys. Update `"sendEmail.success"` value to `"Email terkirim ke {{count}} penerima"` (replacing `{{recipient}}` with `{{count}}`).

    **SendEmailDialog.test.tsx -- update existing + add new tests:**
    - Update renderDialog defaultProps: keep brandRawData with email for pre-fill testing.
    - Update mock for useSendEmail if needed (same mock pattern works).
    - Fix existing tests that reference single recipient input (now EmailChipInput renders differently -- the input is inside a chip container, find by id="send-email-to" or placeholder).
    - Add tests:
      - Pre-fills PIC email as first chip in To field
      - CC link visible, clicking it shows CC field
      - BCC link visible, clicking it shows BCC field
      - Send button disabled when no To recipients
      - Note textarea renders with character count
      - Note textarea respects 500-char limit
      - Preview toggle shows/hides preview section
      - Sends correct payload shape (recipients array, cc, bcc, note)
  </action>
  <verify>
    <automated>cd /Users/mac/HT/Project/aha_sicu/frontend && npx vitest run src/components/dashboard/ --reporter=verbose</automated>
  </verify>
  <done>
    - SendEmailDialog uses EmailChipInput for To/CC/BCC fields
    - CC and BCC fields revealed via Gmail-style text links
    - Custom note textarea with 500-char limit and live character count
    - Preview section loads backend HTML in iframe with note param
    - Dialog scrollable when all fields expanded
    - All existing tests updated, new tests for CC/BCC/note/preview added
    - i18n keys added for all new UI text
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify enhanced send dialog end-to-end</name>
  <files>frontend/src/components/dashboard/SendEmailDialog.tsx</files>
  <action>Human verifies the complete enhanced send dialog end-to-end.</action>
  <what-built>Complete enhanced send dialog: tag/chip email input for multiple recipients, Gmail-style CC/BCC reveal, custom note textarea with character count, and in-dialog email preview with real backend HTML.</what-built>
  <how-to-verify>
    1. Navigate to any brand's evaluation dashboard (PresentationDashboard)
    2. Click "Kirim Email" button in the header
    3. Verify: PIC email appears as a chip in the To field
    4. Type an email address and press Enter -- should create a chip
    5. Type another email and press comma -- should create a chip
    6. Click X on a chip -- should remove it
    7. Click "CC" link next to To label -- CC field should appear, "CC" link should hide
    8. Click "BCC" link -- BCC field should appear, "BCC" link should hide
    9. Add emails to CC and BCC fields
    10. Try adding an 11th recipient total -- should be blocked
    11. Type a note in the "Catatan (opsional)" textarea
    12. Verify character count updates (e.g., "45/500")
    13. Click "Pratinjau Email" toggle -- preview should load in iframe
    14. Verify header/footer images are visible in preview (not broken)
    15. Verify chart shows placeholder text "Chart akan ditampilkan di email"
    16. Verify note appears in preview
    17. Click send -- verify email arrives at all To/CC recipients with the note
    18. Verify BCC recipient receives email but their address is not visible to To/CC recipients
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
  <verify>User confirms all 18 verification steps pass</verify>
  <done>Enhanced send dialog verified working end-to-end with multi-recipient, CC/BCC, note, and preview</done>
</task>

</tasks>

<verification>
cd /Users/mac/HT/Project/aha_sicu/frontend && npx vitest run src/components/dashboard/ --reporter=verbose && npm run lint && tsc --noEmit
</verification>

<success_criteria>
- EmailChipInput component works for all three recipient fields (To, CC, BCC)
- Gmail-style CC/BCC reveal pattern with text links
- Custom note textarea with 500-char limit and live character count
- In-dialog email preview via iframe srcdoc with data URI images
- Preview reflects current note text
- All frontend tests pass
- Human verification confirms full flow works end-to-end
</success_criteria>

<output>
After completion, create `.planning/phases/03-enhanced-send-options/03-02-SUMMARY.md`
</output>
