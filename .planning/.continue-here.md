---
phase: post-milestone
task: 1
total_tasks: 2
status: in_progress
last_updated: 2026-03-06T10:18:29.537Z
---

<current_state>
v1.0 milestone complete and archived. Currently adding SMTP infrastructure to Terraform so the email feature works in production. Terraform files are modified but NOT committed, NOT applied.
</current_state>

<completed_work>

- v1.0 milestone archived (MILESTONES.md, RETROSPECTIVE.md, milestones/ directory, PROJECT.md evolved, ROADMAP.md reorganized, git tag v1.0 pushed)
- Terraform files updated: secrets.tf (smtp_password secret + IAM), cloud_run.tf (7 SMTP env vars), variables.tf (smtp_user, smtp_from_name)
- backend/.env.example updated with SMTP vars and setup instructions
</completed_work>

<remaining_work>

- Task 1: Commit the Terraform + .env.example changes
- Task 2: User needs to do manually:
  1. Set smtp_user in terraform.tfvars (their Gmail address)
  2. Generate Gmail App Password (myaccount.google.com/apppasswords — requires 2-Step Verification)
  3. terraform apply (creates Secret Manager secret)
  4. Inject App Password: echo -n "xxxx xxxx xxxx xxxx" | gcloud secrets versions add aha_sicu_dev_smtp_password --data-file=-
  5. Redeploy Cloud Run to pick up new env vars
  6. For local dev: create backend/.env with SMTP vars and EMAIL_ENABLED=true
</remaining_work>

<decisions_made>

- SMTP password stored in GCP Secret Manager (consistent with existing secrets pattern: db_password, gsheets_credentials, firebase_admin)
- SMTP_HOST and SMTP_PORT as plain env vars (not secrets — they're not sensitive)
- smtp_user as Terraform variable (not secret — it's an email address, not a credential)
- EMAIL_ENABLED=true in Cloud Run (false locally by default for safety)
</decisions_made>

<blockers>
- Gmail App Password availability: user must have 2-Step Verification enabled on their Google account
- If using Google Workspace: admin may need to allow App Passwords for the org
</blockers>

<context>
The milestone is fully complete code-wise. The only gap is infrastructure: the Terraform config to wire SMTP credentials into Cloud Run. The code changes are done and tested — this is purely deployment config.
</context>

<next_action>
Start with: Commit the 4 modified files (backend/.env.example, infrastructure/terraform/cloud_run.tf, secrets.tf, variables.tf), then guide user through the manual steps (tfvars, App Password, terraform apply, secret injection).
</next_action>
