## 1. Implementation

- [x] 1.1 Update the catch block in `useUploadFile` to extract `detail` from structured error objects, using the same pattern as `useRequestSignedUrl` and `useProcessUpload`

## 2. Verify

- [x] 2.1 Run frontend linting and type-check to confirm no regressions
