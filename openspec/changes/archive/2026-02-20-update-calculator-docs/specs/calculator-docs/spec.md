## ADDED Requirements

### Requirement: Documentation-only change — no behavioral specs

This change modifies only `docs/calculator-logic-reference.md`. No application behavior, API contracts, or calculator logic is added, modified, or removed. No specs are required.

#### Scenario: No code changes
- **WHEN** this change is fully applied
- **THEN** no source files in `backend/` or `frontend/` SHALL be modified
