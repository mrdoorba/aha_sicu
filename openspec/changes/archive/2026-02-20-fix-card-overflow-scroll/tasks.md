## 1. Implementation

- [x] 1.1 Add `overflow-hidden` to the `Card` component base className in `frontend/src/components/ui/card.tsx`

## 2. Testing

- [x] 2.1 Add test: wide table inside Card shows horizontal scrollbar (content does not overflow card boundary)
- [x] 2.2 Add test: Card with normal-width content renders without scrollbar or clipping
- [x] 2.3 Run existing frontend tests to verify no regressions
