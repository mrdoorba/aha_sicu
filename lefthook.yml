# Lefthook — git hook manager
# Install: brew install lefthook && lefthook install
# Runs backend lint + frontend lint before push to catch CI failures early.

pre-push:
  parallel: true
  commands:
    backend-lint:
      root: backend/
      run: uv run ruff check .
    frontend-lint:
      root: frontend/
      run: npm run lint
